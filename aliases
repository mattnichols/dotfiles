alias migrate="rake db:migrate db:test:prepare"
alias remigrate="rake db:migrate && rake db:migrate:redo && rake db:schema:dump db:test:prepare"
alias remongrate="rake mongoid:migrate && rake mongoid:migrate:redo"
alias g="git"
alias s="bundle exec rspec"
alias cuc="bundle exec cucumber"
alias gi="gem install"
alias giv="gem install -v"
alias gci="git pull --rebase && rake && git push"
alias tlf="tail -f"
alias b="bundle"
alias be="bundle exec"
alias ln='ln -v'
alias mkdir='mkdir -p'
alias ...='../..'
alias l='ls'
alias ll='ls -al'
alias lh='ls -Alh'
alias m='mate .'

alias -g G='| grep'
alias -g M='| less'
alias -g L='| wc -l'
alias -g ONE="| awk '{ print \$1}'"


###########################################
# Custom Aliases

# aliases
	# Reload Aliases
alias ra="source ~/.zshrc"
	# Edit Aliases
alias ea="mate -w ~/dotfiles/aliases && ra"

# cd
alias ..='cd ..'
alias cdtag="cd ~/Code/tagcow/web"
alias cdgag="cd ~/Code/guessanygame"

# ls
alias ls="ls -F"
alias l="ls -lAh"
alias ll="ls -l"
alias la='ls -A'

# git
alias gl='git pull'
alias gp='git push'
alias gph='git push heroku'
alias gd='git diff'
alias gca='git commit -a'
alias gco='git checkout'
alias gb='git branch'
alias gs='git status'
alias ga='git add .'
alias grm="git status | grep deleted | awk '{print \$3}' | xargs git rm"
function gc() {
	git commit -v -a -m "$*"
}
alias gi="mate .gitignore"
alias gj="gitjour"

# rails
alias sc='script/console'
alias ss='script/server'
alias sg='script/generate'
alias a='autotest -rails'
alias tlog='tail -f -n 100 log/development.log'
alias twlog='tail -f -n 100 /private/var/log/apache2/error_log'
alias scaffold='script/generate nifty_scaffold'
alias migrate='rake db:migrate'
alias rollback='rake db:rollback'
alias sp='script/plugin'
alias rss='script/services_stop;script/services_start;'
alias sss='script/services_stop'
alias rake='be rake'
alias g='bundle exec guard'
alias rc='rails c'
alias rs='unicorn'

# ruby
alias gemlist='gem list | cut -d" " -f1'
alias gems-uninstall-all='gemlist | xargs gem uninstall -Iax'

alias mysqlstart='sudo /opt/local/bin/mysqld_safe5 &'
alias mysqlstop='/opt/local/bin/mysqladmin5 -u root -p shutdown'

# commands starting with % for pasting from web
alias %=' '

alias flushdns='dscacheutil -flushcache'

# heroku
function hcred() {
  #cat ~/.heroku/credentials | grep @
  . ~/.heroku/accounts/$1
}

alias add_dock_space="defaults write com.apple.dock persistent-apps -array-add '{"tile-type"="spacer-tile";}' && killall Dock"
